/**
 * Development environment configuration.
 * For production builds, replace this file via `fileReplacements` in angular.json.
 */
export const environment = {
  production: false,
  // Matches the backend "http" launch profile (Properties/launchSettings.json).
  apiUrl: 'http://localhost:5028/api',
};
