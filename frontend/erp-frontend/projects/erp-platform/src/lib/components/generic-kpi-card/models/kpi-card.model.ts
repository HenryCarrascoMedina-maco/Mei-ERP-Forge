/** Trend direction shown next to a KPI value. */
export type KpiTrend = 'up' | 'down' | 'neutral';

/** Semantic status color for the KPI accent (reuses the badge palette tokens). */
export type KpiStatus = 'success' | 'warning' | 'error' | 'info' | 'neutral' | 'primary';
